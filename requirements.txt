# AlphaTrades Distress Scanner - Requirements
# Python 3.8+

# Core dependencies
requests>=2.31.0
python-dateutil>=2.8.2

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0  # PostgreSQL adapter

# Optional: for async operations (future enhancement)
aiohttp>=3.9.0

# API clients (free tiers available)
# SEC EDGAR - no package needed, uses requests
# Finnhub - no official package, uses requests

# For development/testing
pytest>=7.4.0
pytest-cov>=4.1.0

# For production monitoring (optional)
sentry-sdk>=1.40.0  # Error tracking

# Scheduler (for continuous monitoring)
schedule>=1.2.0
apscheduler>=3.10.0

# Logging
python-json-logger>=2.0.7
